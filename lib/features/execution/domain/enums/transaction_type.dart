enum TransactionType {
  income,
  expense,
}

enum BudgetWarningLevel {
  normal,
  warning,
  danger,
  exceeded,
}

enum InstallmentRequestStatus {
  pending,
  approved,
  rejected,
}

enum CostType {
  total,
  unitBased,
}
